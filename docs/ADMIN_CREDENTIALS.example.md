# Admin credentials — example (copy to local file only)

1. Copy this file to **`docs/ADMIN_CREDENTIALS.md`** (that path is in `.gitignore`).
2. Fill in **only on your machine** — never commit `ADMIN_CREDENTIALS.md`.
3. Prefer a **password manager**; this file is optional.

```markdown
# Local only — DO NOT COMMIT

**Email:** admin@groundchiflow.com  
**Password:** (stored in password manager — entry name: GroundChiFlow admin)

Rotate via Firebase Console → Authentication → Users.
```
